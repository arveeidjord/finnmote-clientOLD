(function () {
	'use strict';

	// function FinnMote() {
	// 	this.server = new app.Server();
    //     this.arrangement = new app.Arrangement(this.server);
    //     this.server.getArragementer();
	// }

    //window.app.server.getArrangementer(window.app.arrangement.loadData);
	// var finnMote = new FinnMote();

	// function setView() {
	// 	todo.controller.setView(document.location.hash);
	// }
	// $on(window, 'load', setView);
	// $on(window, 'hashchange', setView);
})();